/**
 * Service Interfaces layer.
 *
 * <p>Contains service interfaces that define the business operation contracts.
 * Declaring interfaces here and placing implementations in {@code service.impl}
 * enables clean separation, easier unit testing with mocks, and makes it
 * straightforward to swap implementations (e.g., adding caching) without
 * changing callers.
 *
 * <p>Naming convention: {@code JobService}, {@code UserService}, {@code AuthService}
 */
package com.example.ats.service;
