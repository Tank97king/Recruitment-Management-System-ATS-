/**
 * Service Implementations layer.
 *
 * <p>Contains {@code @Service}-annotated implementation classes for each
 * service interface defined in {@code com.example.ats.service}.
 *
 * <p>This is where ALL business logic lives:
 * <ul>
 *   <li>Calling repositories to read/write data</li>
 *   <li>Enforcing business rules (throwing exceptions when violated)</li>
 *   <li>Mapping between entities and DTOs via MapStruct mappers</li>
 *   <li>Coordinating transactions with {@code @Transactional}</li>
 * </ul>
 *
 * <p>Naming convention: {@code JobServiceImpl}, {@code UserServiceImpl}
 */
package com.example.ats.service.impl;
